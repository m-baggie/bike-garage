{
  "name": "bike-garage",
  "version": "1.0.0",
  "description": "Bike photo analysis app using Claude Vision and Performance Bicycle pricing",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "lint": "npm run lint --prefix client",
    "test": "npm test --prefix server && npm test --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
